"""
Пакет media: скачивание медиа из Web/Telegram во временную директорию.

Гарантии:
    * Все файлы кладутся в config.TMP_DIR.
    * Имя файла — случайный uuid (никаких коллизий, никаких user-controlled имён).
    * Cleanup — отдельной функцией cleanup(): вызывается после отправки админу.
"""
